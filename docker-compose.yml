version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    container_name: backend
  
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    container_name: frontend